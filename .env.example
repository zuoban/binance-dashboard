# ============================================================================
# 币安合约交易信息查看系统 - 环境变量配置
# ============================================================================
#
# 使用说明：
# 1. 复制此文件为 .env.local：cp .env.example .env.local
# 2. 填写你的币安 API 密钥（见下方说明）
# 3. 不要将 .env.local 提交到 Git！
#
# 安全提示：
# - 只使用具有"只读"权限的 API 密钥
# - 不要启用交易、提现等任何高风险权限
# - 定期轮换 API 密钥
# - 在生产环境使用环境变量管理密钥
# ============================================================================

# ------------------------------------------------------------------------------
# 币安 API 配置（必填）
# ------------------------------------------------------------------------------

# 币安 API Key（客户端公开）
# 用途：标识请求来源
# 获取方式：币安账户 -> API 管理 -> 创建 API Key
# 权限要求：仅读取权限
NEXT_PUBLIC_BINANCE_API_KEY=your_api_key_here

# 币安 API Secret（服务端私密）
# 用途：请求签名，验证身份
# 重要：此变量仅在服务端可用，客户端无法访问！
BINANCE_API_SECRET=your_secret_key_here

# ------------------------------------------------------------------------------
# API 端点配置（可选，通常使用默认值）
# ------------------------------------------------------------------------------

# 币安合约 REST API 端点
# 默认值：https://fapi.binance.com（生产环境）
# 测试网：https://testnet.binancefuture.com
NEXT_PUBLIC_BINANCE_REST_API=https://fapi.binance.com

# 币安合约 WebSocket API 端点
# 默认值：wss://fstream.binance.com/ws（生产环境）
# 测试网：wss://stream.binancefuture.com/ws
NEXT_PUBLIC_BINANCE_WS_API=wss://fstream.binance.com/ws

# ------------------------------------------------------------------------------
# 应用配置（可选）
# ------------------------------------------------------------------------------

# 应用访问地址
# 开发环境：http://localhost:3000
# 生产环境：https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 运行环境
# 可选值：development | production | test
NODE_ENV=development

# ------------------------------------------------------------------------------
# 高级配置（可选）
# ------------------------------------------------------------------------------

# 是否使用测试网
# 设置为 "true" 使用币安测试网
# BINANCE_TESTNET=true

# 日志级别（开发环境）
# 可选值：debug | info | warn | error
# LOG_LEVEL=info

# ------------------------------------------------------------------------------
# 部署平台配置说明
# ------------------------------------------------------------------------------
#
# Vercel 部署：
# 1. 进入项目 Settings -> Environment Variables
# 2. 添加以下环境变量：
#    - NEXT_PUBLIC_BINANCE_API_KEY
#    - BINANCE_API_SECRET
#    - NEXT_PUBLIC_BINANCE_REST_API（可选）
#    - NEXT_PUBLIC_BINANCE_WS_API（可选）
#    - NEXT_PUBLIC_APP_URL（部署后设置为实际域名）
#
# 其他平台：
# 参考对应平台的文档设置环境变量
#
# ============================================================================
